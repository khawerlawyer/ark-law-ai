{
  "name": "ark-law-ai",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": { "start": "node server.js" },
  "dependencies": {
    "express": "^4.18.0",
    "node-fetch": "^2.6.7"
  }
}
```
Click **"Commit new file"**

---

**File 2:** Name it `server.js` — paste the full code from the **server.js artifact** above. Click **"Commit new file"**

---

**File 3:** Name it `Procfile` (no extension) — paste this:
```
web: node server.js
